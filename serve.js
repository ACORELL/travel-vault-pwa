// This file is not part of the phone PWA architecture.
// The PWA is a static app — host the pwa/phone/ folder on any static server
// (GitHub Pages, Netlify, etc.). Once installed via the browser's
// "Add to Home Screen", it works fully offline with no server needed.
//
// The vault folder lives on the phone's local storage (managed by Syncthing).
// The PWA accesses it directly via the File System Access API.
